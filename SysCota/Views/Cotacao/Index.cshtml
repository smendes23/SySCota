@model IEnumerable<SysCota.Models.Cotacao>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeroDaCotacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataCotacao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataEntregaCotacao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroDaCotacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataCotacao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataEntregaCotacao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
